import { NavBar } from "../navbar/navbar"
import { HeaderStyled } from "./header.styled"

export const Header = () => {
    return(
        <HeaderStyled>
            <NavBar />
        </HeaderStyled>
    )
}